build/
install/
log/
*.pyc
__pycache__/
.vscode/
.cursor/
*.bag
*.csv
!report/appendix/*.csv
scripts/output/
third_party/*
# Vendored NLopt source (MIT) — fp_bspline_opt builds against it; the
# nlopt_install/ prefix stays ignored (build output).
!third_party/nlopt/
third_party/nlopt/build/

.ros_log_tmp/
.ros_logs/

# Path H / G training artifacts (large; regenerate locally)
*.pt
*.pid
src/drone_rl_planner/checkpoints/*.log
src/drone_rl_planner/checkpoints/*status*.json
src/drone_rl_planner/checkpoints/*.zip
src/drone_rl_planner/checkpoints/*_replay/
src/drone_rl_planner/checkpoints/_restart_*.txt
src/drone_rl_planner/checkpoints/_restart_*.sh

# Local UI assets / heavy caches
src/drone_bringup/drone_bringup/dashboard_static/backgrounds/
src/drone_bringup/drone_bringup/dashboard_static/vendor/
src/drone_bringup/drone_bringup/dashboard_static/bg-15-sequoia-sunrise.png
report/map_smoke_logs/
report/batch_matrix/
report/*.log
report/acceptance_runs/**/launch.log
report/baseline/*.log

# Large third-party mirrors (clone separately if needed)
reference_repos/FUEL/

# Extra RL weight dumps
src/drone_rl_planner/checkpoints/*.npz
src/drone_rl_planner/runs/

# Vendor notebooks (large)
src/mighty_vendor/**/*.ipynb

# Planner benchmark raw trial dumps (charts/summaries are enough)
report/planner_benchmark/runs/
report/planner_benchmark/live_*.log
report/planner_benchmark/watch_status.txt

# LaTeX build intermediates (keep .tex/.pdf/figures)
report/final_paper/*.aux
report/final_paper/*.log
report/final_paper/*.fls
report/final_paper/*.fdb_latexmk
report/final_paper/*.xdv
report/final_paper/*.toc
report/final_paper/*.out
report/final_paper/*.synctex.gz
report/final_paper/figures/maps/_*.log


# Full-resolution GIF backups (too large for GitHub)
docs/media/*_full.gif

# Raw screen captures / oversized local encodes (use Bilibili for demo)
*.mov
*.MOV
docs/media/demo.mp4
